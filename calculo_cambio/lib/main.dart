import 'package:calculo_cambio/pages/home.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    title: 'Flutter Demo',
    theme: ThemeData(hintColor: Colors.amber, primaryColor: Colors.amber),
    home: const Home(),
  ));
}